#exercicios-app {
  font-family: Arial, sans-serif;
  padding: 15px;
  background-color: #fff0f5;
  border-radius: 15px;
  max-width: 400px;
  margin: 10px auto;
}

h2 {
  color: #ff69b4;
  text-align: center;
}

ul {
  list-style: none;
  padding: 0;
}

li {
  margin: 10px 0;
  font-size: 16px;
}
/* --- 2. HEADER E FOTO --- */
.app-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 0;
    margin-bottom: 20px;
    border-bottom: 2px solid var(--cor-secundaria);
}

.app-header h1 {
    color: var(--cor-primaria);
    font-size: 1.8em;
    margin: 0;
}

.app-header p {
    color: var(--cor-texto);
    margin: 0;
    font-size: 0.9em;
}

.photo-container {
    width: 80px;
    height: 80px;
    border-radius: 50%; /* Foto Circular! */
    overflow: hidden;
    box-shadow: var(--sombra-suave);
    border: 3px solid var(--cor-primaria); /* Borda Fofa */
}

.profile-photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
}